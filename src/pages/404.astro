---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Página não encontrada`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Erro</span>
          <span class="text-primary">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">Ops! Não encontramos a página que você procura.</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Mas não se preocupe, você pode voltar à página inicial e explorar nossos serviços e conteúdos.
        </p>
        <a
          rel="noopener noreferrer"
          href={getHomePermalink()}
          class="btn-secondary py-3 px-6 font-semibold shadow-none text-base w-auto bg-isaGreen text-offWhite hover:bg-isaGreen/80"
        >
          Voltar para a página inicial
        </a>
      </div>
    </div>
  </section>
</Layout>
